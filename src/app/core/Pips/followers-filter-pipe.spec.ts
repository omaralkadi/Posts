import { FollowersFilterPipe } from './followers-filter-pipe';

describe('FollowersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FollowersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
